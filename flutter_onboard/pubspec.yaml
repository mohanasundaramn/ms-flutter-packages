name: flutter_onboard
description: A simple, elegant and easy to use onboard widget for both android
  and ios devices(Null-Safety).
version: 2.0.0
homepage: https://github.com/mohanasundaramn/ms-flutter-packages
repository: https://github.com/mohanasundaramn/ms-flutter-packages/tree/master/flutter_onboard

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.18.0
  freezed_annotation: ^0.15.0
  hooks_riverpod: ^2.1.3

dev_dependencies:
  build_runner: ^2.0.6
  flutter_test:
    sdk: flutter
  freezed: ^0.15.0+1
  lint: ^1.0.0
flutter: null
